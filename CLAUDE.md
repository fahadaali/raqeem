# دليل العمل على منصة رقيم

هذا الملف يقرؤه وكيل الذكاء الاصطناعي قبل أي تعديل على المستودع.

---

## ١ — الهوية البصرية: ملزمة بلا استثناء

**المرجع الكامل: [`docs/IDENTITY.md`](docs/IDENTITY.md) — اقرأه قبل أي عمل على الواجهة.**

كل شاشة أو مكوّن أو نصّ واجهة جديد يلتزم به حرفياً. وهذه خلاصته:

| القاعدة | التطبيق |
|---|---|
| **الألوان** | من توكنز `:root` في `web/css/app.css` وحدها. لا قيمة لون مباشرة في أي مكوّن. |
| **النِّسَب** | قشدي ٤٥٪ · سنا ٢٥٪ · غامق ١٥٪ · **مشمشي ≤ ٧٪**. المشمشي للإجراء الرئيسي ونقاط الإنجاز فقط، وليس لون خلفيات. |
| **الخطوط** | Zain للعناوين (`--font-head`)، IBM Plex Sans Arabic للنصوص (`--font-body`). لا خط ثالث. |
| **الأيقونات** | Lucide حصراً عبر `web/js/icons.js`. لا رموز تعبيرية، ولا محارف طباعية (✓ ✕ ★ ▲) مكان الأيقونات. |
| **الاتجاه** | RTL بالكامل. خصائص منطقية فقط (`margin-inline-*`, `inset-inline-*`, `text-align:start`) — لا `left/right`. |
| **الشعار** | ملفات `web/assets/brand/` تُستعمل كما هي. لا تُعدَّل ولا يُعاد رسمها في أي مكان. |
| **النمط الزخرفي** | النجمة الثمانية **مضبَّبةً** بشفافية منخفضة. ولا يُكتب `url(pattern…)` مباشرةً: يُؤخذ `--glass-on-brand` أو `--glass-on-ink` أو `--glass-on-light`. الضبابية مخبوزة في `pattern-soft-*.svg` ويولّدها `npm run gen:pattern` — والفاحص يسقط إن طُبع النمط عارياً أو حادّاً. |
| **النبرة** | عربية فصيحة هادئة موقّرة تناسب بيئة حلقات القرآن. لا رموز تعبيرية في نصوص الواجهة ولا في عناوين الإشعارات. |

### قبل كل دمج

```bash
npm run verify:identity     # فحص الانحراف عن الهوية
npm run verify:wiring       # اكتمال ربط المزايا
BASE=http://localhost:3000 npm run verify   # تدقيق تكاملي (يحتاج خادماً يعمل)
```

### مكوّنات مشتركة تُستعمل ولا يُعاد بناؤها

| المكوّن | متى |
|---|---|
| `picker()` في `web/js/util.js` | **كل** قائمة يُختار منها شخص — فيها حقل بحث. بديلٌ مباشر لـ `select` (`.value` و`onchange`) |
| `userOptions(list, { group })` | تحويل منسوبين إلى خيارات المنتقي — الاسم سطراً والدور تحته |
| `.tbl.sheet` + `.cellx` | جدولٌ يُحرَّر في مكانه. ولا تُسمَّ حالةُ جدولٍ `.grid`: الكلاس يضع `display:grid` فيكسر الجدول |

### أين تُضاف أيقونة جديدة

`web/js/icons.js` — من حزمة لوسايد وحدها، بهندستها كما تصدرها دون تعديل، والاسم
بصيغته الأصلية (kebab-case). ثم تُستعمل هكذا:

```js
el('button.btn', { icon: 'save', text: 'حفظ', onclick })
el('span.ic', { icon: 'inbox', iconSize: 'card' })
```

---

## ٢ — بنية المستودع

| ماذا | أين |
|---|---|
| واجهة المستخدم (تطبيق ويب مثبَّت، بلا إطار عمل) | `web/js/` — `app.js` الهيكل، `views/` الشاشات |
| هيكل لوحة المنصة (طبقة مستقلة) | `web/js/admin-shell.js` + `web/js/views/admin/` |
| نظام التصميم | `web/css/app.css` |
| نظام الأيقونات | `web/js/icons.js` |
| أصول الهوية | `web/assets/brand/` |
| منطق العمل المشترك (Node + Workers) | `server/core/` |
| مشغّل الخادم الذاتي | `server/node/` |
| مشغّل Cloudflare | `server/worker/` |
| الترحيلات | `migrations/` |
| الأدوات والفحوص | `scripts/` |

الواجهة تُبنى بـ `el()` في `web/js/util.js` — منشئ عناصر DOM لا قوالب نصية،
فما يكتبه المستخدم يُعرَض نصّاً دائماً ولا يُنفَّذ.

---

## ٣ — حدود التعديل

- **منطق العمل** (`server/core/`، الصلاحيات، الاعتمادات، الفوترة، التدقيق) لا
  يُمَسّ إلا حين يكون هو المطلوب صراحةً.
- **مستندات الطباعة والتصدير** (`server/core/report.js`، `xlsx.js`) واجهاتٌ أيضاً:
  تلتزم الهوية كما تلتزمها الشاشات.
- **البيانات الافتراضية** (`server/core/seed.js`، `provision.js`، `landing.js`)
  تحمل ألوان الهوية وأسماء أيقونات لوسايد — لا ألواناً ولا رموزاً خارجها.
- **أيقونات التطبيق** تُولَّد من `web/assets/brand/app-icon.svg` بأمر
  `npm run gen:icons` — لا تُحرَّر كصور ولا تُرسم يدوياً.
- **لقطات الصفحة الرئيسية** (`web/assets/screens/`) تُولَّد من المنصّة نفسها بأمر
  `npm run gen:screens` وهو يحتاج خادماً يعمل — لا تُلتقط باليد ولا تُقصّ في محرّر
  صور. وأيُّ تغييرٍ في شاشةٍ منها يُعاد به التوليد، وإلّا شاخت اللقطة عن واجهتها.

---

## ٤ — اللغة

المستودع عربي: التعليقات، أسماء الشاشات، رسائل الطرفية، ونصوص الواجهة. اكتب
التعليقات الجديدة بالعربية بنفس نبرة ما حولها — تشرح **لماذا** لا **ماذا**.
